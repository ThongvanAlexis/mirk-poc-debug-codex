---
phase: 01-foundation-and-assets
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - tool/check_headers.dart
  - tool/check_licenses.dart
  - tool/check_dependencies_md.dart
  - tool/test/check_headers_test.dart
  - tool/test/check_licenses_test.dart
  - tool/test/check_dependencies_md_test.dart
  - DEPENDENCIES.md
  - .github/workflows/ci.yml
  - test/ci/ci_workflow_test.dart
autonomous: true
requirements: [FOUND-02, FOUND-03, FOUND-04, FOUND-05, CI-01]
must_haves:
  truths:
    - "CI rejects formatting, analyzer, test, GOSL header, license, telemetry, and dependency-table regressions."
    - "The license gate fails closed on GPL, AGPL, LGPL, SSPL, unknown licenses, and known telemetry or analytics SDK packages unless explicitly allowlisted."
    - "DEPENDENCIES.md includes a telemetry column and stays synchronized with pubspec.lock."
    - "Phase 1 CI does not build or upload APK/IPA artifacts."
  artifacts:
    - "tool/check_headers.dart, tool/check_licenses.dart, and tool/check_dependencies_md.dart."
    - "tool/test/ tests for all three guard scripts."
    - "DEPENDENCIES.md with direct and transitive dependency audit rows for the current lockfile."
    - ".github/workflows/ci.yml with a CI-01 gates job only."
    - "test/ci/ci_workflow_test.dart asserting the required workflow steps and excluding artifact jobs."
  key_links:
    - "pubspec.lock -> DEPENDENCIES.md via tool/check_dependencies_md.dart."
    - ".dart_tool/package_config.json -> tool/check_licenses.dart after flutter pub get."
    - ".github/workflows/ci.yml -> same local commands used in plan verification."
---

<objective>
Install the policy gates and GitHub Actions lint workflow that keep the foundation compliant as later phases add map, fog, permission, logging, and artifact work.

Purpose: Phase 1 must make compliance executable, not just documented: strict formatting/analyze, required headers, dependency license/telemetry checks, dependency table freshness, and tests all run in CI.
Output: Guard scripts, audit document, workflow, and workflow tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-assets/01-CONTEXT.md
@.planning/phases/01-foundation-and-assets/01-RESEARCH.md
@.planning/phases/01-foundation-and-assets/01-VALIDATION.md
@C:\claude_checkouts\GOSL-MirkFall\tool\check_headers.dart
@C:\claude_checkouts\GOSL-MirkFall\tool\check_licenses.dart
@C:\claude_checkouts\GOSL-MirkFall\tool\check_dependencies_md.dart
@C:\claude_checkouts\mirk-poc-debug\.github\workflows\ci.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port and adapt policy guard scripts with tests</name>
  <files>tool/check_headers.dart, tool/check_licenses.dart, tool/check_dependencies_md.dart, tool/test/check_headers_test.dart, tool/test/check_licenses_test.dart, tool/test/check_dependencies_md_test.dart</files>
  <behavior>
    - Header gate passes only when hand-written Dart files under `lib/`, `test/`, `tool/`, and `integration_test/` start with the exact GOSL three-line header.
    - License gate rejects GPL, AGPL, LGPL, SSPL, unknown licenses, and known telemetry/analytics/ad SDK package names unless an explicit reviewed allowlist entry exists.
    - Dependency markdown gate reports missing, extra, or version-mismatched rows under Direct, Dev, and Transitive sections.
  </behavior>
  <action>
    Port or adapt the parent/sibling scripts named in D-12 rather than reimplementing from scratch. Preserve the useful parent patterns: BOM-aware header reads, generated-file exclusions, structured YAML parsing, manual license overrides with comments, and section-aware `DEPENDENCIES.md` parsing.

    Adapt the license gate for this repo's constraints from D-13: fail GPL, AGPL, LGPL, SSPL, unknown license IDs, and known telemetry/analytics/ad SDK package names such as Firebase Analytics, Crashlytics, Sentry, PostHog, Mixpanel, Amplitude, Segment, AppsFlyer, Adjust, ad SDKs, and MapLibre packages. Keep manual overrides narrow and commented with source/rationale.
  </action>
  <verify>
    <automated>dart test tool/test/</automated>
    <automated>dart run tool/check_headers.dart</automated>
  </verify>
  <done>All guard script tests pass, and the header gate passes against the current repo.</done>
</task>

<task type="auto">
  <name>Task 2: Seed dependency audit document and prove freshness</name>
  <files>DEPENDENCIES.md</files>
  <action>
    After `flutter pub get`, inspect `pubspec.lock` and `.dart_tool/package_config.json`. Create `DEPENDENCIES.md` with sections for Direct dependencies, Dev dependencies, and Transitive dependencies. Every non-SDK package in `pubspec.lock` must have a row with package, version, license, source, telemetry, transitive licenses, maintenance note, platform, and audit date.

    The Telemetry column must make the no-automatic-network-egress review explicit. Use "None" only after reviewing package role/source; user-initiated platform behavior must be stated as such. Unknown licenses or unresolved packages should cause `tool/check_licenses.dart` or `tool/check_dependencies_md.dart` to fail until the row or override is corrected.
  </action>
  <verify>
    <automated>dart run tool/check_licenses.dart</automated>
    <automated>dart run tool/check_dependencies_md.dart</automated>
  </verify>
  <done>`DEPENDENCIES.md` is synchronized with `pubspec.lock`, includes telemetry review, and both policy gates pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the CI-01 GitHub Actions gates job only</name>
  <files>.github/workflows/ci.yml, test/ci/ci_workflow_test.dart</files>
  <action>
    Create `.github/workflows/ci.yml` with the D-15 gates job that runs on push, pull_request, and workflow_dispatch. Pin `subosito/flutter-action@v2` to Flutter `3.41.7` on stable. The job must run, in this order or a clearly equivalent order: checkout, Flutter setup, `flutter pub get`, `dart format --line-length 160 --set-exit-if-changed .`, `flutter analyze --fatal-infos --fatal-warnings`, `dart run tool/check_headers.dart`, `dart run tool/check_licenses.dart`, `dart run tool/check_dependencies_md.dart`, `dart test tool/test/`, and `flutter test`.

    Do not add Android APK or iOS IPA artifact jobs in this phase; D-16 and the roadmap assign those to Phase 3. Add `test/ci/ci_workflow_test.dart` that parses the workflow text and asserts required gate commands are present and that artifact upload/build jobs are absent.
  </action>
  <verify>
    <automated>flutter test test/ci/ci_workflow_test.dart</automated>
    <automated>dart format --line-length 160 --set-exit-if-changed .; flutter analyze --fatal-infos --fatal-warnings; dart test tool/test/; flutter test; dart run tool/check_headers.dart; dart run tool/check_licenses.dart; dart run tool/check_dependencies_md.dart</automated>
  </verify>
  <done>The CI workflow represents CI-01 exactly, local full gates pass, and workflow tests fail if Phase 3 artifact jobs are added prematurely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pub resolver -> dependency policy | `pubspec.lock` can introduce transitive packages not visible in direct dependencies. |
| GitHub Actions -> project quality gate | CI becomes the automated enforcement surface for local policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-06 | Information Disclosure | Dependency graph | mitigate | License gate rejects known telemetry/analytics/ad SDK packages and requires telemetry review in `DEPENDENCIES.md`. |
| T-1-07 | Tampering | Dependency licenses | mitigate | `check_licenses.dart` scans lockfile package roots after `flutter pub get` and fails unknown/disallowed licenses. |
| T-1-08 | Repudiation | Dependency audit drift | mitigate | `check_dependencies_md.dart` requires every lockfile package to appear with matching version in `DEPENDENCIES.md`. |
| T-1-09 | Denial of Service | CI workflow scope creep | mitigate | Workflow test asserts Phase 1 gates only and rejects premature artifact jobs that belong to Phase 3. |
</threat_model>

<verification>
Run:
- `flutter pub get`
- `dart test tool/test/`
- `dart run tool/check_headers.dart`
- `dart run tool/check_licenses.dart`
- `dart run tool/check_dependencies_md.dart`
- `flutter test test/ci/ci_workflow_test.dart`
- `dart format --line-length 160 --set-exit-if-changed .`
- `flutter analyze --fatal-infos --fatal-warnings`
- `flutter test`
</verification>

<success_criteria>
- Guard scripts and their tests exist and pass.
- `DEPENDENCIES.md` includes all lockfile packages and telemetry review.
- CI workflow includes every CI-01 gate command.
- CI workflow excludes Android APK and iOS IPA artifact jobs.
- Full local gate suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-assets/01-03-SUMMARY.md`.
</output>
