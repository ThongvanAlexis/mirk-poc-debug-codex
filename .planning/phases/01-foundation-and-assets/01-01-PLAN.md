---
phase: 01-foundation-and-assets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - pubspec.lock
  - analysis_options.yaml
  - LICENSE
  - .gitignore
  - lib/main.dart
  - android/app/build.gradle.kts
  - android/app/src/main/AndroidManifest.xml
  - ios/Runner/Info.plist
  - ios/Runner.xcodeproj/project.pbxproj
  - test/platform/platform_metadata_test.dart
  - test/widget_test.dart
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03, FOUND-04]
must_haves:
  truths:
    - "A Flutter iOS+Android app scaffold exists with package name mirk_poc_debug and user-visible name MirkFall POC."
    - "iOS metadata is SideStore-safe before any sideload: bundle identifier com.thongvan.mirkPocDebug and CFBundleName MirkPocDebug."
    - "Strict analyzer settings are active from the first Dart source commit."
    - "All committed Dart files created in this plan start with the required GOSL header."
  artifacts:
    - "pubspec.yaml with exact Phase 1 dependency pins and no caret ranges for direct dependencies."
    - "pubspec.lock committed after flutter pub get."
    - "analysis_options.yaml with strict-casts, strict-inference, and strict-raw-types."
    - "LICENSE copied from the parent GOSL v1.0 license."
    - "test/platform/platform_metadata_test.dart asserting app identity and SideStore-sensitive metadata."
  key_links:
    - "pubspec.yaml -> pubspec.lock via flutter pub get."
    - "analysis_options.yaml -> flutter analyze --fatal-infos --fatal-warnings."
    - "platform metadata -> test/platform/platform_metadata_test.dart static assertions."
---

<objective>
Create the mobile-only Flutter foundation that later phases can build on without changing package identity, SideStore-sensitive metadata, analyzer strictness, or the GOSL baseline.

Purpose: Phase 1 starts by making the repository a valid Flutter app with the exact identity and policy settings locked in `01-CONTEXT.md` decisions D-01 through D-04, D-10, D-17, and D-18.
Output: Flutter scaffold, strict analyzer config, committed lockfile, GOSL license, and platform metadata tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-assets/01-CONTEXT.md
@.planning/phases/01-foundation-and-assets/01-RESEARCH.md
@.planning/phases/01-foundation-and-assets/01-VALIDATION.md
@C:\claude_checkouts\GOSL-MirkFall\LICENSE.md
@C:\claude_checkouts\mirk-poc-debug\analysis_options.yaml
@C:\claude_checkouts\mirk-poc-debug\docs\flutter-ios-specifics.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the mobile Flutter app with locked identity</name>
  <files>pubspec.yaml, pubspec.lock, .gitignore, LICENSE, lib/main.dart, android/app/build.gradle.kts, android/app/src/main/AndroidManifest.xml, ios/Runner/Info.plist, ios/Runner.xcodeproj/project.pbxproj</files>
  <action>
    If the checkout is still only planning files, run `flutter create --org com.thongvan --project-name mirk_poc_debug --platforms ios,android .` per D-04. If a scaffold already exists, do not re-run `flutter create`; inspect and surgically adjust it.

    Set `pubspec.yaml` to exact direct pins only, with no caret ranges per D-10: Flutter SDK, `cupertino_icons: 1.0.9`, `path_provider: 2.1.5`, `path: 1.9.1`, `crypto: 3.0.7`, `flutter_lints: 6.0.0`, `yaml: 3.1.3`, and `test: 1.30.0`. Do not add `flutter_map`, `vector_map_tiles`, `vector_map_tiles_pmtiles`, `geolocator`, `permission_handler`, logging, or share packages in this plan; D-11 requires a resolver-coherent map chain if map packages are added, and map rendering belongs to Phase 2.

    Copy the parent `LICENSE.md` to repo-root `LICENSE`. Replace the default `lib/main.dart` with a GOSL-headered tiny app shell that can be extended by Plan 02. Patch iOS and Android metadata per D-01, D-02, and D-17: Dart package `mirk_poc_debug`, iOS bundle identifier `com.thongvan.mirkPocDebug`, Android application ID `com.thongvan.mirk_poc_debug`, display name `MirkFall POC`, `CFBundleName` `MirkPocDebug`, and `ITSAppUsesNonExemptEncryption=false`. Keep platforms to iOS and Android only per D-03.
  </action>
  <verify>
    <automated>flutter pub get</automated>
    <automated>if (Select-String -Path pubspec.yaml -Pattern '\^' -Quiet) { exit 1 }</automated>
    <automated>flutter test test/widget_test.dart</automated>
  </verify>
  <done>Flutter project files exist, `pubspec.lock` is created and committed, app identity values match D-01/D-02, and no direct dependency uses a caret range.</done>
</task>

<task type="auto">
  <name>Task 2: Enable strict Dart analysis and required headers from the baseline</name>
  <files>analysis_options.yaml, lib/main.dart, test/widget_test.dart</files>
  <action>
    Create `analysis_options.yaml` using the sibling POC pattern and D-18: include `package:flutter_lints/flutter.yaml`, enable `strict-casts`, `strict-inference`, and `strict-raw-types`, treat `missing_required_param`, `missing_return`, and `use_build_context_synchronously` as errors, ignore `todo`, and exclude generated `*.g.dart` / `*.freezed.dart` files.

    Ensure every `.dart` file created so far starts with exactly the D-14 header:
    `// Copyright (c) 2026 THONGVAN Alexis`
    `// Licensed under the Good Old Software License v1.0`
    `// See LICENSE file for details`

    Format with line length 160. Do not wait for the header tool from Plan 03 to catch files created here; this task owns the baseline compliance.
  </action>
  <verify>
    <automated>dart format --line-length 160 --set-exit-if-changed lib test</automated>
    <automated>flutter analyze --fatal-infos --fatal-warnings</automated>
  </verify>
  <done>Strict analysis passes with fatal warnings/infos, and all current hand-written Dart files have the required header.</done>
</task>

<task type="auto">
  <name>Task 3: Add static platform metadata assertions</name>
  <files>test/platform/platform_metadata_test.dart, test/widget_test.dart</files>
  <action>
    Add `test/platform/platform_metadata_test.dart` with GOSL header. It should read platform files as plain text/XML and assert the Phase 1 identity contract: `com.thongvan.mirkPocDebug` appears in the iOS project, `com.thongvan.mirk_poc_debug` appears in Android Gradle config, `CFBundleName` is exactly `MirkPocDebug`, `CFBundleDisplayName` is `MirkFall POC`, `CFBundleName` contains no `_` or `-`, and `ITSAppUsesNonExemptEncryption` is false.

    Keep the default widget test small and aligned with the app shell. Do not introduce a polished UI or permission/map/fog behavior in this plan.
  </action>
  <verify>
    <automated>flutter test test/platform/platform_metadata_test.dart test/widget_test.dart</automated>
    <automated>flutter analyze --fatal-infos --fatal-warnings</automated>
  </verify>
  <done>Automated tests fail if package identity or SideStore-sensitive metadata drifts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer workstation -> generated platform scaffold | `flutter create` writes many managed files; manual edits must be constrained to known metadata files. |
| App identity -> SideStore/App-ID registration | Bundle identifier and `CFBundleName` become external app identity values during sideloading. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Spoofing | iOS/Android app identity | mitigate | Static metadata tests assert bundle ID, app ID, display name, and safe `CFBundleName` before later phases sideload. |
| T-1-02 | Tampering | Dart source baseline | mitigate | Strict analyzer, format command, and required GOSL headers apply from the first source commit. |
| T-1-03 | Repudiation | Dependency resolution | mitigate | Commit `pubspec.lock` immediately after `flutter pub get`; Plan 03 adds dependency freshness checks. |
</threat_model>

<verification>
Run:
- `flutter pub get`
- `dart format --line-length 160 --set-exit-if-changed lib test`
- `flutter analyze --fatal-infos --fatal-warnings`
- `flutter test test/platform/platform_metadata_test.dart test/widget_test.dart`
</verification>

<success_criteria>
- Mobile-only Flutter scaffold exists and can run.
- `pubspec.yaml` has exact Phase 1 pins and `pubspec.lock` is committed.
- iOS and Android identity values match D-01/D-02.
- `analysis_options.yaml` enables strict Dart language settings.
- Current `.dart` files have the required GOSL header.
- Platform metadata tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-assets/01-01-SUMMARY.md`.
</output>
