---
phase: 02-same-pipeline-map-and-fog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - pubspec.lock
  - DEPENDENCIES.md
  - assets/shaders/atmospheric_fog.frag
  - lib/config/constants.dart
  - test/assets/shader_asset_test.dart
  - test/config/constants_test.dart
autonomous: true
requirements: [MAP-03, FOG-01, FOG-02, FOG-05, FOG-07]
must_haves:
  truths:
    - "The Phase 2 package graph is resolver-coherent: flutter_map 7.0.2 with vector_map_tiles 8.0.0 and vector_map_tiles_pmtiles 1.5.0."
    - "All new direct dependencies are exact-pinned and pass the existing license, telemetry, and dependency-table gates."
    - "The MirkFall atmospheric fog shader is copied unchanged to assets/shaders/atmospheric_fog.frag and declared under flutter.shaders."
    - "Map, PMTiles source-key, Melun camera, reveal radius, SDF resolution, and atmospheric fog constants exist in lib/config/constants.dart."
    - "No MapLibre, Mapbox, analytics, telemetry, GPL, AGPL, LGPL, or SSPL dependency is introduced."
  artifacts:
    - "pubspec.yaml with Phase 2 exact pins and shader asset declaration."
    - "pubspec.lock after flutter pub get."
    - "DEPENDENCIES.md updated for every non-SDK lockfile package."
    - "assets/shaders/atmospheric_fog.frag copied from parent MirkFall."
    - "test/assets/shader_asset_test.dart and test/config/constants_test.dart."
  key_links:
    - "pubspec.yaml -> pubspec.lock via flutter pub get."
    - "pubspec.lock -> DEPENDENCIES.md via tool/check_dependencies_md.dart."
    - "assets/shaders/atmospheric_fog.frag -> future FogShaderUniforms slot layout."
---

<objective>
Install the resolver-coherent Phase 2 map/fog package graph, copy the real atmospheric shader, and add the constants/tests that downstream map and fog plans depend on.

Purpose: Phase 2 cannot start by writing map/fog widgets against an impossible solver graph. This plan deliberately resolves the `flutter_map 8.3.0` project-note mismatch by pinning the stable PMTiles-compatible chain found in local package source: `flutter_map 7.0.2`, `vector_map_tiles 8.0.0`, and `vector_map_tiles_pmtiles 1.5.0`.

Output: dependency graph, shader asset, constants, and audit documents ready for Plans 02-02 through 02-04.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-same-pipeline-map-and-fog/02-RESEARCH.md
@.planning/phases/02-same-pipeline-map-and-fog/02-VALIDATION.md
@.planning/phases/02-same-pipeline-map-and-fog/02-PATTERNS.md
@C:\claude_checkouts\GOSL-MirkFall\assets\shaders\atmospheric_fog.frag
@C:\claude_checkouts\GOSL-MirkFall\lib\config\constants.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the exact-pinned map/fog dependency graph</name>
  <files>pubspec.yaml, pubspec.lock, DEPENDENCIES.md</files>
  <action>
    Add these exact direct dependency pins, with no caret ranges:
    - `flutter_map: 7.0.2`
    - `vector_map_tiles: 8.0.0`
    - `vector_map_tiles_pmtiles: 1.5.0`
    - `vector_tile_renderer: 5.2.0`
    - `pmtiles: 1.2.0`
    - `latlong2: 0.9.1`
    - `geolocator: 14.0.2`
    - `logging: 1.3.0`

    Keep existing Phase 1 pins unchanged. Run `flutter pub get`. If the solver rejects this graph, stop and inspect the exact resolver output; do not "solve" by adopting beta packages unless the resulting graph is documented and license-clean. After lockfile generation, update `DEPENDENCIES.md` so the dependency freshness gate passes.
  </action>
  <verify>
    <automated>flutter pub get</automated>
    <automated>if (Select-String -Path pubspec.yaml -Pattern '\^' -Quiet) { exit 1 }</automated>
    <automated>dart run tool/check_licenses.dart</automated>
    <automated>dart run tool/check_dependencies_md.dart</automated>
  </verify>
  <done>The lockfile resolves with stable map/fog packages, no direct dependency uses caret syntax, and dependency policy gates pass.</done>
</task>

<task type="auto">
  <name>Task 2: Copy and declare the atmospheric shader asset</name>
  <files>assets/shaders/atmospheric_fog.frag, pubspec.yaml, test/assets/shader_asset_test.dart</files>
  <action>
    Copy `C:\claude_checkouts\GOSL-MirkFall\assets\shaders\atmospheric_fog.frag` to `assets/shaders/atmospheric_fog.frag` unchanged. Declare it under `flutter.shaders` in `pubspec.yaml`.

    Add `test/assets/shader_asset_test.dart` with the required GOSL Dart header. The test should read `pubspec.yaml` as text, assert the shader path is listed under `shaders:`, and use `rootBundle.loadString(kPocFogShaderAssetPath)` to assert the asset is present and contains the scalar SDF rect uniforms `uSdfRectOriginX`, `uSdfRectOriginY`, `uSdfRectSizeX`, and `uSdfRectSizeY`.
  </action>
  <verify>
    <automated>flutter test test/assets/shader_asset_test.dart</automated>
    <automated>dart run tool/check_headers.dart</automated>
  </verify>
  <done>The shader asset is bundled, declared, and structurally checked by a focused asset test.</done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 2 constants and tests</name>
  <files>lib/config/constants.dart, test/config/constants_test.dart</files>
  <action>
    Extend `lib/config/constants.dart` with Phase 2 constants while preserving the existing PMTiles constants:
    - Map source key: `kPocTileProviderSourceKey = 'protomaps'`
    - Initial camera: lat `48.5397`, lon `2.6553`, zoom `13`
    - Recenter zoom `15`
    - Optional min/max zoom and padded Melun bounds for local map navigation
    - `kPocFogShaderAssetPath = 'assets/shaders/atmospheric_fog.frag'`
    - `kPocRevealDiscRadiusMeters = 25.0`
    - `kEarthRadiusMeters = 6371008.8`
    - `kMetersPerDegreeLat = 111320.0`
    - Atmospheric palette, drift, scale, opacity, curl, light, hue, boundary, SDF resolution constants listed in `.planning/PROJECT.md`

    Add `test/config/constants_test.dart` to lock the renderer-critical values: Melun camera, source key, reveal radius, SDF resolution 256, shader asset path, and identity SDF rect defaults if constants are exposed for them.
  </action>
  <verify>
    <automated>flutter test test/config/constants_test.dart</automated>
    <automated>dart format --line-length 160 --set-exit-if-changed lib test</automated>
    <automated>flutter analyze --fatal-infos --fatal-warnings</automated>
  </verify>
  <done>Renderer-critical constants are present, tested, formatted, and analyzer-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pub resolver -> app binary | New direct/transitive packages enter the app. |
| Parent shader source -> POC shader asset | Product-critical shader code is copied into this repository. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Information Disclosure | Dependency graph | mitigate | Existing license/telemetry gate must pass after adding map, location, and logging packages. |
| T-2-02 | Tampering | Shader asset | mitigate | Copy from parent, declare in `pubspec.yaml`, and add an asset test that checks critical uniform names. |
| T-2-03 | Denial of Service | Dependency solver | mitigate | Pin the stable resolver-coherent graph; stop on solver failure instead of adopting beta packages silently. |
</threat_model>

<verification>
Run:
- `flutter pub get`
- `flutter test test/assets/shader_asset_test.dart test/config/constants_test.dart`
- `dart run tool/check_headers.dart`
- `dart run tool/check_licenses.dart`
- `dart run tool/check_dependencies_md.dart`
- `dart format --line-length 160 --set-exit-if-changed .`
- `flutter analyze --fatal-infos --fatal-warnings`
</verification>

<success_criteria>
- Phase 2 package graph resolves and is exact-pinned.
- `DEPENDENCIES.md` is synchronized with `pubspec.lock` and telemetry review remains explicit.
- Atmospheric shader asset is copied and listed under `flutter.shaders`.
- Phase 2 constants cover map camera, source key, reveal radius, SDF resolution, and atmospheric defaults.
- No code references MapLibre or overlay compensation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-same-pipeline-map-and-fog/02-01-SUMMARY.md`.
</output>
