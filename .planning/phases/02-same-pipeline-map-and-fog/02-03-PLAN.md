---
phase: 02-same-pipeline-map-and-fog
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
files_modified:
  - lib/domain/mirk/mirk_viewport_bbox.dart
  - lib/domain/revealed/reveal_disc.dart
  - lib/infrastructure/mirk/animation_helpers.dart
  - lib/infrastructure/mirk/mirk_projection.dart
  - lib/infrastructure/mirk/sdf/revealed_sdf_builder.dart
  - lib/infrastructure/mirk/sdf/sdf_cache.dart
  - lib/infrastructure/mirk/shader/fog_shader_uniforms.dart
  - lib/presentation/widgets/fog_clip_path.dart
  - test/domain/mirk/mirk_viewport_bbox_test.dart
  - test/domain/revealed/reveal_disc_test.dart
  - test/infrastructure/mirk/animation_helpers_test.dart
  - test/infrastructure/mirk/mirk_projection_test.dart
  - test/infrastructure/mirk/sdf/revealed_sdf_builder_test.dart
  - test/infrastructure/mirk/sdf/sdf_cache_test.dart
  - test/infrastructure/mirk/shader/fog_shader_uniforms_test.dart
  - test/presentation/widgets/fog_clip_path_test.dart
autonomous: true
requirements: [FOG-02, FOG-04, FOG-05, FOG-06, FOG-07]
must_haves:
  truths:
    - "RevealDisc, viewport bbox, projection, clip path, SDF builder, uniform layout, and animation helper are ported from MirkFall with package-name adaptation only where needed."
    - "SDF builds a 256x256 midpoint-128 RGBA image using metre-space distance, not pixel/degree distance."
    - "Fog clip path subtracts reveal-disc circles from the viewport using the same metre-space projection convention as the SDF builder."
    - "FogShaderUniforms writes exactly 41 float slots and binds the SDF image sampler at index 0."
    - "Identity SDF rect `(0, 0, 1, 1)` remains the default for the same-pipeline test."
  artifacts:
    - "Ported fog domain/infrastructure files under lib/domain, lib/infrastructure/mirk, and lib/presentation/widgets."
    - "Focused unit/widget tests for geometry, SDF bytes, clip paths, animation, caching, and shader uniforms."
  key_links:
    - "RevealDisc.intersectsBbox -> RevealedSdfBuilder.buildFromDiscs -> FogLayer."
    - "MirkProjection and fog_clip_path -> current FlutterMap camera viewport."
    - "FogShaderUniforms -> atmospheric_fog.frag uniform declaration order."
---

<objective>
Port the battle-tested MirkFall fog math and shader binding surfaces so Plan 04 can add a same-Canvas `FogLayer` without reimplementing renderer-critical algorithms under UI pressure.

Purpose: The POC is about render pipeline synchronization, not inventing new fog/SDF math. This plan creates the tested fog infrastructure in isolation before it is mounted into the map.

Output: parent-derived reveal geometry, 256x256 SDF builder/cache, clip path, shader uniform setter, animation helper, and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-same-pipeline-map-and-fog/02-RESEARCH.md
@.planning/phases/02-same-pipeline-map-and-fog/02-VALIDATION.md
@.planning/phases/02-same-pipeline-map-and-fog/02-PATTERNS.md
@.planning/phases/02-same-pipeline-map-and-fog/02-01-SUMMARY.md
@C:\claude_checkouts\GOSL-MirkFall\lib\domain\revealed\reveal_disc.dart
@C:\claude_checkouts\GOSL-MirkFall\lib\domain\mirk\mirk_viewport_bbox.dart
@C:\claude_checkouts\GOSL-MirkFall\lib\infrastructure\mirk\sdf\revealed_sdf_builder.dart
@C:\claude_checkouts\GOSL-MirkFall\lib\infrastructure\mirk\tile_cell_iteration.dart
@C:\claude_checkouts\GOSL-MirkFall\lib\infrastructure\mirk\mirk_projection.dart
@C:\claude_checkouts\GOSL-MirkFall\lib\infrastructure\mirk\shader\fog_shader_uniforms.dart
@C:\claude_checkouts\GOSL-MirkFall\lib\infrastructure\mirk\animation_helpers.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port reveal disc and viewport bbox domain types</name>
  <files>lib/domain/revealed/reveal_disc.dart, lib/domain/mirk/mirk_viewport_bbox.dart, test/domain/revealed/reveal_disc_test.dart, test/domain/mirk/mirk_viewport_bbox_test.dart</files>
  <behavior>
    - `RevealDisc` is immutable, has id/sessionId/lat/lon/radiusMeters/fixedAtUtc, asserts radius > 0 if assertions are used, and exposes `distanceMetersTo` plus `intersectsBbox`.
    - `MirkViewportBbox` is a tiny immutable class with south/west/north/east and asserts `south <= north`; antimeridian support can be preserved from parent or omitted only with a documented POC reason.
    - No Freezed, generated files, Drift, or parent session model is introduced.
  </behavior>
  <action>
    Copy the useful parent code and adapt imports/package names. Replace Freezed viewport with a handwritten class. Keep the Haversine and bbox-intersection math from parent, including `kEarthRadiusMeters` and `kMetersPerDegreeLat`.
  </action>
  <verify>
    <automated>flutter test test/domain/revealed/reveal_disc_test.dart test/domain/mirk/mirk_viewport_bbox_test.dart</automated>
  </verify>
  <done>Domain geometry compiles without generated code and tests cover Haversine, bbox intersection, equality, and invalid bbox/radius behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port SDF builder, projection, and clip path</name>
  <files>lib/infrastructure/mirk/sdf/revealed_sdf_builder.dart, lib/infrastructure/mirk/mirk_projection.dart, lib/presentation/widgets/fog_clip_path.dart, test/infrastructure/mirk/sdf/revealed_sdf_builder_test.dart, test/infrastructure/mirk/mirk_projection_test.dart, test/presentation/widgets/fog_clip_path_test.dart</files>
  <behavior>
    - Empty disc list builds all-fog SDF bytes.
    - Single disc inside viewport creates revealed bytes near center and fog bytes near corners.
    - A disc outside viewport leaves all-fog bytes.
    - At Melun latitude, a 25 m disc remains circular in metre-space, defending against degree/pixel ovals.
    - Clip path equals viewport rect minus reveal-disc circles and agrees visually/geometrically with SDF projection.
  </behavior>
  <action>
    Port `RevealedSdfBuilder`, `MirkProjection`, and `buildViewportFogClipPathFromDiscs` or a named equivalent. Keep the 256x256 square resolution and midpoint-128 RGBA encoding. If `ui.decodeImageFromPixels` makes pure unit tests awkward, add small image-sampling helpers like the parent tests use rather than reducing coverage.
  </action>
  <verify>
    <automated>flutter test test/infrastructure/mirk/sdf/revealed_sdf_builder_test.dart test/infrastructure/mirk/mirk_projection_test.dart test/presentation/widgets/fog_clip_path_test.dart</automated>
  </verify>
  <done>SDF/projection/clip path code is ported and tests defend metre-space geometry plus the all-fog and single-disc cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Port shader uniforms, animation helper, and SDF cache</name>
  <files>lib/infrastructure/mirk/shader/fog_shader_uniforms.dart, lib/infrastructure/mirk/animation_helpers.dart, lib/infrastructure/mirk/sdf/sdf_cache.dart, test/infrastructure/mirk/shader/fog_shader_uniforms_test.dart, test/infrastructure/mirk/animation_helpers_test.dart, test/infrastructure/mirk/sdf/sdf_cache_test.dart</files>
  <behavior>
    - `FogShaderUniforms.totalFloatSlots == 41`.
    - Uniform setter writes slots 0 through 40 in the order documented by `atmospheric_fog.frag`.
    - `setImageSampler(0, sdfImage)` is called for the SDF sampler.
    - SDF rect defaults used by downstream code are identity `(0.0, 0.0, 1.0, 1.0)`.
    - `triangleWave` returns min at t=0, max at half-period, min at full-period, and handles non-positive period.
    - SDF cache prevents duplicate concurrent builds for the same disc/viewport key and disposes old images when appropriate.
  </behavior>
  <action>
    Port `FogShaderUniforms` and `triangleWave` directly from parent. Add a small `SdfCache` that caches by a deterministic key derived from disc snapshot and viewport. Keep it simple: POC correctness beats a complex eviction policy. Tests can use a fake SDF builder to avoid expensive image work for cache semantics.
  </action>
  <verify>
    <automated>flutter test test/infrastructure/mirk/shader/fog_shader_uniforms_test.dart test/infrastructure/mirk/animation_helpers_test.dart test/infrastructure/mirk/sdf/sdf_cache_test.dart</automated>
    <automated>dart format --line-length 160 --set-exit-if-changed lib test</automated>
    <automated>flutter analyze --fatal-infos --fatal-warnings</automated>
  </verify>
  <done>Shader uniform, animation, and SDF cache surfaces are tested and ready for FogLayer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GPS/reveal geometry -> shader sampler | Domain coordinates become an SDF image consumed by GPU shader code. |
| Parent source -> POC implementation | Battle-tested code is adapted across package boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-07 | Tampering | Reveal/SDF geometry | mitigate | Unit tests defend metre-space distance and SDF byte semantics. |
| T-2-08 | Denial of Service | SDF rebuilds | mitigate | Cache SDF builds and avoid duplicate in-flight rebuilds for identical inputs. |
| T-2-09 | Tampering | Shader uniform layout | mitigate | Slot-count/order tests preserve the 41 float layout and sampler index. |
</threat_model>

<verification>
Run:
- `flutter test test/domain/revealed test/domain/mirk test/infrastructure/mirk test/presentation/widgets/fog_clip_path_test.dart`
- `dart format --line-length 160 --set-exit-if-changed lib test`
- `flutter analyze --fatal-infos --fatal-warnings`
- `dart run tool/check_headers.dart`
</verification>

<success_criteria>
- Ported fog code compiles without generated files or parent persistence layers.
- SDF builder produces 256x256 midpoint-128 images and tests sample expected bytes.
- Clip path and SDF use consistent metre-space projection.
- Uniform layout matches the shader declaration and identity SDF rect remains the downstream default.
</success_criteria>

<output>
After completion, create `.planning/phases/02-same-pipeline-map-and-fog/02-03-SUMMARY.md`.
</output>
