---
phase: 03-mobile-runtime-and-artifacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - pubspec.lock
  - DEPENDENCIES.md
  - ios/Podfile
  - ios/Runner/Info.plist
  - ios/Runner/PrivacyInfo.xcprivacy
  - android/app/src/main/AndroidManifest.xml
  - test/platform/platform_metadata_test.dart
autonomous: true
requirements: [CI-04, CI-05]
must_haves:
  truths:
    - "permission_handler and share_plus are exact-pinned direct dependencies and pass the existing license, telemetry, and dependency-table gates."
    - "The iOS Podfile is committed with PERMISSION_LOCATION=1 in GCC_PREPROCESSOR_DEFINITIONS before any Dart permission request can ship."
    - "iOS Info.plist keeps CFBundleName as MirkPocDebug and adds NSLocationWhenInUseUsageDescription only."
    - "Android declares only foreground fine/coarse location permissions; no background location, foreground service, notification, or always-location metadata is introduced."
    - "PrivacyInfo.xcprivacy declares FileTimestamp reason C617.1 and UserDefaults reason CA92.1."
    - "Context decisions covered: D-02, D-03, D-05, D-23, D-24."
  artifacts:
    - "Updated pubspec.yaml, pubspec.lock, and DEPENDENCIES.md."
    - "ios/Podfile with permission_handler location macro."
    - "ios/Runner/PrivacyInfo.xcprivacy."
    - "Extended platform metadata tests."
  key_links:
    - "permission_handler Dart calls in Plan 03-03 -> ios/Podfile PERMISSION_LOCATION=1 from this plan."
    - "FileLogger stat/prune in Plan 03-02 -> PrivacyInfo.xcprivacy FileTimestamp declaration from this plan."
    - "Foreground GPS stream in Plan 03-03 -> Info.plist and AndroidManifest foreground permission strings from this plan."
---

<objective>
Create the dependency and platform metadata foundation required for Phase 3 foreground permissions, share-log support, and Apple privacy compliance.

Purpose: The POC must not reach runtime code with an iOS permission no-op or a SideStore/privacy metadata blocker. This plan installs the package and platform contract before the app asks for location or shares logs.

Output: exact-pinned permission/share packages, committed iOS Podfile macro, foreground-only platform permissions, privacy manifest, and static tests that guard the contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mobile-runtime-and-artifacts/03-CONTEXT.md
@.planning/phases/03-mobile-runtime-and-artifacts/03-UI-SPEC.md
@.planning/phases/03-mobile-runtime-and-artifacts/03-RESEARCH.md
@.planning/phases/03-mobile-runtime-and-artifacts/03-PATTERNS.md
@C:\claude_checkouts\mirk-poc-debug\docs\flutter-ios-specifics.md
@pubspec.yaml
@DEPENDENCIES.md
@ios/Runner/Info.plist
@android/app/src/main/AndroidManifest.xml
@test/platform/platform_metadata_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exact-pinned permission and share dependencies</name>
  <files>pubspec.yaml, pubspec.lock, DEPENDENCIES.md</files>
  <action>
    Add exact direct dependency pins for:
    - `permission_handler: 12.0.1`
    - `share_plus: 12.0.2`

    Preserve all existing exact pins and do not add caret ranges. Run `flutter pub get`, then update `DEPENDENCIES.md` for every new non-SDK package in `pubspec.lock`, including telemetry notes. If the solver requires a newer `share_plus`, stop and document the exact version choice before updating the lockfile and audit table.
  </action>
  <verify>
    <automated>flutter pub get</automated>
    <automated>if (Select-String -Path pubspec.yaml -Pattern '\^' -Quiet) { exit 1 }</automated>
    <automated>dart run tool/check_licenses.dart</automated>
    <automated>dart run tool/check_dependencies_md.dart</automated>
  </verify>
  <done>Runtime permission/share dependencies resolve, remain exact-pinned, and dependency policy gates pass.</done>
</task>

<task type="auto">
  <name>Task 2: Commit foreground-only iOS and Android permission metadata</name>
  <files>ios/Podfile, ios/Runner/Info.plist, android/app/src/main/AndroidManifest.xml, test/platform/platform_metadata_test.dart</files>
  <action>
    Add a committed `ios/Podfile` if one does not exist. It must include the standard Flutter Podfile shape plus a `post_install` block that appends `PERMISSION_LOCATION=1` to `GCC_PREPROCESSOR_DEFINITIONS` for all build configurations.

    Update `ios/Runner/Info.plist` with `NSLocationWhenInUseUsageDescription` matching the POC purpose. Keep `CFBundleName` as `MirkPocDebug`; do not add always-location usage strings or `UIBackgroundModes`.

    Update `android/app/src/main/AndroidManifest.xml` with `ACCESS_FINE_LOCATION` and `ACCESS_COARSE_LOCATION` before `<application>`. Do not add `ACCESS_BACKGROUND_LOCATION`, `FOREGROUND_SERVICE`, `FOREGROUND_SERVICE_LOCATION`, or `POST_NOTIFICATIONS`.

    Extend `test/platform/platform_metadata_test.dart` to assert all of the above and to reject the forbidden background permission strings.
  </action>
  <verify>
    <automated>flutter test test/platform/platform_metadata_test.dart</automated>
    <automated>dart run tool/check_headers.dart</automated>
  </verify>
  <done>Foreground permission metadata is present, SideStore-safe, and guarded by static tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add Apple required-reason privacy manifest</name>
  <files>ios/Runner/PrivacyInfo.xcprivacy, ios/Runner.xcodeproj/project.pbxproj, test/platform/platform_metadata_test.dart</files>
  <action>
    Add `ios/Runner/PrivacyInfo.xcprivacy` declaring:
    - `NSPrivacyAccessedAPICategoryFileTimestamp` with reason `C617.1`
    - `NSPrivacyAccessedAPICategoryUserDefaults` with reason `CA92.1`

    Ensure the file is included in the iOS Runner target if the current Xcode project requires explicit resource membership. Extend `test/platform/platform_metadata_test.dart` to assert the file exists and contains both required reason codes.
  </action>
  <verify>
    <automated>flutter test test/platform/platform_metadata_test.dart</automated>
    <automated>dart format --line-length 160 --set-exit-if-changed test</automated>
    <automated>flutter analyze --fatal-infos --fatal-warnings</automated>
  </verify>
  <done>Required-reason API declarations are committed and covered by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pub resolver -> app binary | New direct/transitive packages enter the runtime build. |
| Dart permission request -> iOS build settings | Runtime permission code depends on native preprocessor macros. |
| App runtime APIs -> Apple privacy review | File timestamp and UserDefaults usage must be declared for iOS artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Denial of Service | iOS permission prompt | mitigate | Commit Podfile `PERMISSION_LOCATION=1` and test it statically. |
| T-3-02 | Information Disclosure | Dependency graph | mitigate | Exact pins plus license/telemetry dependency gates. |
| T-3-03 | Compliance | iOS privacy manifest | mitigate | Add required-reason API manifest and tests before artifact builds. |
</threat_model>

<verification>
Run:
- `flutter pub get`
- `flutter test test/platform/platform_metadata_test.dart`
- `dart run tool/check_licenses.dart`
- `dart run tool/check_dependencies_md.dart`
- `dart run tool/check_headers.dart`
- `dart format --line-length 160 --set-exit-if-changed .`
- `flutter analyze --fatal-infos --fatal-warnings`
</verification>

<success_criteria>
- `permission_handler` and `share_plus` are exact-pinned and audit-clean.
- iOS has `PERMISSION_LOCATION=1`, the when-in-use usage string, and no background location metadata.
- Android has fine/coarse foreground location permissions only.
- `PrivacyInfo.xcprivacy` exists with FileTimestamp `C617.1` and UserDefaults `CA92.1`.
- Platform metadata tests fail on missing macros, missing usage strings, forbidden background permissions, or missing privacy reasons.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-runtime-and-artifacts/03-01-SUMMARY.md`.
</output>
